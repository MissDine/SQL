# SQL-Structures querry language-language used to comuunicate with data bases
# It is used by many different database systems such as MySQL, PostgreSQL
#  SQL systems like Postgres, or MySQ
# A database is a place where you can store your data
# sql lite is a lighter version of sql
# Data types in sql-null,interger,text(string),real,blob
# Null  no value
# TEXT-string
# Interger-whole number
# Real-float,decimal numbers
# Blob-eg images
# SQLite allows the programmer to use other common datatypes outside of the four mentioned above. We can refer to TEXT INTEGER REAL BLOB as datatype "categories".
# Boolean values are also stored as integers (0 for false, 1 for true).

# Primary & foreign key
# primary-a unique key eg usernames, passwords should be unique
# foreign-a unique key that links another thing

# SQL OPERATIONS
# Create table -Create row Insert,Select,Join,Update,Delete,Count,Add,Sum,Avg, Alter table,drop table
# Relational Databases like SQLite store data in a structure we refer to as a table. 
# AGGREGATE FUNTIONS -Aggregate functions perform a calculation on specified values, queried from a database table.Sum, Max, Min, AVG, Count
# SELECT AVG(column_name) FROM table_name;
# RELATIONAL DATABASE-Is a data base that has relations-has more than one table-The tables must have a relationship
#  One to mant relationship-one parent, many children
# Primary Key: a column in a table with an identifier (ID) that uniquely identifies one specific record, or row, in a table
# Foreign Key: a column in from one table that refers to a primary key in another table
# Database-collection of various tables in a single container
# SQL joins-commands used to combine rows from two or more tables based on the a related column between those two tables.#
# Types of join-inner join,left join, right join,full join.

# .header on
# .mode column
# DISTINCT- to select all the unique/unreeated stuff